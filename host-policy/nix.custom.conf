# FlexNetOS/Yazelix strict local-build policy.
# Kache is the only persistent compilation cache; Nix substitutes are disabled.

always-allow-substitutes = false
compress-build-log = false
keep-build-log = false
substitute = false
substituters =
trusted-public-keys =
trusted-substituters =
trusted-users = root flexnetos
