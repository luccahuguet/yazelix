// ========================================
// USER ZELLIJ CONFIGURATION TEMPLATE
// ========================================
// This is a template file with documentation and examples.
// 
// To customize Zellij:
// 1. Copy this directory: cp -r configs/zellij/user configs/zellij/personal
// 2. Edit files in configs/zellij/personal/ to customize your settings
// 3. Your personal configs are git ignored - no conflicts on updates!
//
// Configuration layers (in order of precedence):
//   1. Zellij defaults (from `zellij setup --dump-config`)
//   2. Yazelix overrides (yazelix_overrides.kdl) 
//   3. Your settings (personal/user_config.kdl) - HIGHEST PRIORITY
//
// IMPORTANT: Merging uses simple concatenation. Zellij uses the last occurrence
// of any setting. For nested blocks (ui, keybinds, themes), avoid duplicates
// by overriding the entire section rather than adding partial blocks.
//
// Documentation: https://zellij.dev/documentation/
// ========================================

// ========================================
// COMMON CUSTOMIZATIONS
// ========================================
// Uncomment and modify the sections you want to customize.
// Remove the '//' at the beginning of lines to enable them.

// === THEME AND UI ===
// Choose a built-in theme or define your own
theme "dracula"
// theme "nord"
// theme "tokyo-night"

// Disable mouse support if it interferes with terminal selection
// mouse_mode false

// Increase scrollback buffer for more history
// scroll_buffer_size 50000

// Use simplified UI without arrow fonts for better compatibility
// simplified_ui true

// === UI CUSTOMIZATION ===
// Yazelix doesn't define UI settings, so you can safely add them here:
// ui {
//     pane_frames {
//         rounded_corners true
//         hide false
//     }
// }

// === COPY/PASTE BEHAVIOR ===
// Automatically copy selection when releasing mouse (default: true) 
// copy_on_select false

// Use primary clipboard instead of system clipboard (Linux/X11)
// copy_clipboard "primary"

// Custom copy command (overrides Yazelix default of "wl-copy")
// copy_command "xclip -selection clipboard"  // For X11
// copy_command "pbcopy"                      // For macOS

// === SESSION BEHAVIOR ===
// Show startup tips when Zellij starts (controlled via disable_zellij_tips in yazelix.toml)
// show_startup_tips false  // Uncomment to override yazelix.toml setting

// What to do when terminal is closed: "detach" or "quit"
// on_force_close "quit"

// Enable session serialization for persistence across restarts
// session_serialization true

// === CUSTOM KEYBINDINGS ===
// RECOMMENDED: For small keybinding changes, edit the layout files directly:
//   - configs/zellij/layouts/yazelix.kdl
//   - configs/zellij/layouts/yazelix_no_sidebar.kdl
//
// Only use this section if you want to completely replace all keybindings:

// keybinds {
//     normal {
//         // Example: Add custom binding for floating panes
//         bind "Alt t" { ToggleFloatingPanes; }
//         
//         // NOTE: This replaces ALL keybindings, including Yazelix defaults
//         // You'll need to redefine any bindings you want to keep
//     }
// }

// === CUSTOM THEMES ===
// Define your own color schemes. Safe to uncomment - adds new themes
// without conflicting with existing ones.

// themes {
//     my_custom_theme {
//         fg 248 248 242
//         bg 40 42 54  
//         red 255 85 85
//         green 80 250 123
//         yellow 241 250 140
//         blue 98 114 164
//         magenta 255 121 198
//         orange 255 184 108
//         cyan 139 233 253
//         black 0 0 0
//         white 255 255 255
//     }
// }

// === PLUGINS ===
// Load custom plugins or override plugin settings
// load_plugins {
//     "https://github.com/user/my-plugin/releases/latest/download/my-plugin.wasm"
// }

// Override plugin configurations
// plugins {
//     // Example: Customize the tab bar plugin
//     tab-bar location="zellij:tab-bar" {
//         // Plugin-specific configuration here
//     }
// }

// ========================================
// ADVANCED CONFIGURATION
// ========================================

// === LAYOUT SETTINGS ===
// Override the default layout (Yazelix sets this to "yazelix")
// default_layout "compact"

// Disable automatic layout management
// auto_layout false

// === EDITOR INTEGRATION ===
// Override scrollback editor (Yazelix sets this to "hx")
// scrollback_editor "nvim"
// scrollback_editor "code --wait"

// === DEVELOPMENT/DEBUG ===
// Enable more verbose logging
// Additional settings for development work

// ========================================
// RAW KDL CONFIGURATION
// ========================================
// Any additional KDL configuration can be added below.
// This is useful for very advanced customizations or new Zellij features
// not yet covered by the examples above.
