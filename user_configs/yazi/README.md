# Yazi

Put optional user-managed Yazi overrides here

- `yazi.toml` adds Yazi settings that Yazelix does not own
- `keymap.toml` adds keybindings
- `init.lua` adds custom Lua setup

Yazelix keeps `[opener].edit` owned so file opens continue targeting the managed editor pane
