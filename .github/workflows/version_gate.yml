name: Version Gate

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: version-gate-${{ github.workflow }}-${{ github.run_id }}
  cancel-in-progress: false

jobs:
  linux-profile-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    defaults:
      run:
        shell: bash
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v22
        with:
          determinate: false
          extra-conf: |
            max-jobs = 2
            cores = 2

      - name: Install yzn into a temp profile
        run: |
          profile="$RUNNER_TEMP/yazelix-next-profile"
          runtime_profile="$RUNNER_TEMP/yazelix-next-runtime-profile"
          nix profile add --refresh "$GITHUB_WORKSPACE" --profile "$profile"
          nix profile add --refresh "$GITHUB_WORKSPACE#runtime" --profile "$runtime_profile"

          test -x "$profile/bin/yzn"
          test -f "$profile/share/applications/yzn.desktop"
          grep -F 'Yazelix Nova' "$profile/share/applications/yzn.desktop"
          test -x "$runtime_profile/bin/yzn"
          test ! -e "$runtime_profile/share/applications/yzn.desktop"

      - name: Smoke installed yzn commands
        run: |
          profile="$RUNNER_TEMP/yazelix-next-profile"
          export HOME="$RUNNER_TEMP/yazelix-next-home"
          export YAZELIX_NEXT_CONFIG_HOME="$RUNNER_TEMP/yazelix-next-config"
          export YAZELIX_STATE_DIR="$RUNNER_TEMP/yazelix-next-state"
          export XDG_DATA_HOME="$RUNNER_TEMP/yazelix-next-data"
          mkdir -p "$HOME" "$YAZELIX_NEXT_CONFIG_HOME" "$YAZELIX_STATE_DIR" "$XDG_DATA_HOME"

          "$profile/bin/yzn" help > "$RUNNER_TEMP/yzn-help.txt"
          "$profile/bin/yzn" status > "$RUNNER_TEMP/yzn-status.txt"
          "$profile/bin/yzn" doctor > "$RUNNER_TEMP/yzn-doctor.txt"
          "$profile/bin/yzn" tutor list > "$RUNNER_TEMP/yzn-tutor-list.txt"

          grep -F 'Yazelix Nova' "$RUNNER_TEMP/yzn-help.txt"
          grep -F 'Yazelix Nova status' "$RUNNER_TEMP/yzn-status.txt"
          grep -F "config home: $YAZELIX_NEXT_CONFIG_HOME" "$RUNNER_TEMP/yzn-status.txt"
          grep -F "state dir: $YAZELIX_STATE_DIR" "$RUNNER_TEMP/yzn-status.txt"
          grep -F 'Yazelix Nova doctor' "$RUNNER_TEMP/yzn-doctor.txt"
          grep -F "ok config home: $YAZELIX_NEXT_CONFIG_HOME" "$RUNNER_TEMP/yzn-doctor.txt"
          grep -F 'Yazelix Nova tutor lessons' "$RUNNER_TEMP/yzn-tutor-list.txt"

  macos-package-smoke:
    runs-on: macos-15
    timeout-minutes: 90
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
    defaults:
      run:
        shell: bash
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v22
        with:
          determinate: false
          extra-conf: |
            max-jobs = 1
            cores = 2

      - name: Build aarch64-darwin packages and Home Manager closure
        run: |
          full="$(nix build --no-link --no-write-lock-file --print-out-paths --print-build-logs .#packages.aarch64-darwin.yzn)"
          runtime="$(nix build --no-link --no-write-lock-file --print-out-paths --print-build-logs .#packages.aarch64-darwin.runtime)"
          nix build --no-link --no-write-lock-file --print-build-logs .#checks.aarch64-darwin.home_manager

          test ! -e "$full/share/applications/yzn.desktop"
          test ! -e "$runtime/share/applications/yzn.desktop"
