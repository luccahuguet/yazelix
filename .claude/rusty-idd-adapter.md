# Rusty IDD adapter — claude

GENERATED by `rusty-idd render claude`. Do not hand-edit; `rusty-idd render --check` (CI) fails on drift.

`.claude/` is a THIN ADAPTER (ADR-0010, ADR-0015). The workflow — rules, gates, stage packages, the artifact DAG — lives in the Rusty IDD engine, not here.

Before any work:
1. Run `rusty-idd next` (human) or `rusty-idd next --json` (programmatic).
2. Do exactly what it reports; re-run to advance.

Do not add always-loaded workflow prose to this directory.
