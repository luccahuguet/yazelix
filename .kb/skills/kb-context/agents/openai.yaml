interface:
  display_name: "KB Context"
  short_description: "Load and validate project context"
  default_prompt: "Use $kb-context to load project context following the AGENTS.md PATH A/B/C flow."
policy:
  allow_implicit_invocation: true
